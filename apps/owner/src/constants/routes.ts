const ROUTES = {
  main: '/',
  progress: '/progress',
  bid: '/bid',
  feed: '/feed',
  reservation: '/reservation',
  profile: '/profile',
  map: '/map',
  search: '/search',
} as const;

export default ROUTES;
